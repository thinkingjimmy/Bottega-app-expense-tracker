# gui/

> L2 | Parent: ../README.md

- src/: React interface, Tailwind stylesheet, pure business projections, and shadcn components.
- components.json: Host shadcn component selection and aliases.
- component-origins.json: Signed component provenance.
- .bottega/: Immutable upstream component merge bases.

Bottega 0.1.3 compiles the default React export with `bottega-react-v1`. The GUI reads complete Base snapshots and opens the existing data views; row mutations remain in Base and Use chat.

[PROTOCOL]: Update this header when making changes, then check README.md.
